class Solution:
    def firstUniqChar(self, s: str) -> int:
        if s == "":
            return -1
        for i in s:
            if s.count(i)==1:
                return s.index(i)
                break
        return -1
